[build]
  command = "cd blog-source && npm install && npm run build && cd .. && cp -r blog-source/out blog"
  functions = "netlify/functions"
  publish = "."

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/chat"
  to = "/.netlify/functions/chat"
  status = 200
